<%= bootstrap_form_for(bank_slip) do |form| %>
  <% if bank_slip.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bank_slip.errors.count, "error") %> prohibited this bank_slip from being saved:</h2>

      <ul>
        <% bank_slip.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :bar_code %>
  </div>

  <div>
    <%= form.select :client_id, Client.all.map{|t| [t.name, t.id]} %>
  </div>

  <div>
    <%= form.select :payer_id, Client.all.map{|t| [t.name, t.id]} %>
  </div>

  <div>
    <%= form.date_field :due_date %>
  </div>

  <div>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
